const menu=document.querySelector('.menu');const nav=document.querySelector('.nav nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}window.addEventListener('scroll',()=>{const header=document.querySelector('.nav');if(header)header.style.background=window.scrollY>50?'#050505ee':'linear-gradient(#050505dd, transparent)'});const memberGrid=document.getElementById('memberGrid');if(memberGrid){[['ST','TVA Stalin'],['NI','TVA Ninja'],['AD','TVA Alex Duvor']].forEach(([avatar,name])=>{if([...memberGrid.querySelectorAll('.member h3')].some(h=>h.textContent.trim()===name))return;const card=document.createElement('div');card.className='member';card.innerHTML=`<div class="avatar">${avatar}</div><div class="member-main"><div class="member-top"><b>00</b><span class="status">● ACTIVE</span></div><h3>${name}</h3><span class="member-role">MEMBER</span></div>`;memberGrid.appendChild(card)})}const search=document.getElementById('memberSearch');const count=document.getElementById('memberCount');const members=[...document.querySelectorAll('.member')];members.forEach((card,i)=>{const number=card.querySelector('.member-top b');if(number)number.textContent=String(i+1).padStart(2,'0')});const rosterTotal=members.length;const rosterStats=document.querySelector('.roster-stats div strong');if(rosterStats)rosterStats.textContent=rosterTotal;const heroText=document.querySelector('.players-hero-inner h1 + p');if(heroText)heroText.textContent=`${rosterTotal} members. One family. One legacy.`;if(count)count.textContent=`${rosterTotal} PLAYERS`;search?.addEventListener('input',()=>{const q=search.value.toLowerCase().trim();let shown=0;members.forEach(card=>{const match=card.textContent.toLowerCase().includes(q);card.style.display=match?'flex':'none';if(match)shown++});if(count)count.textContent=`${shown} ${shown===1?'PLAYER':'PLAYERS'}`});document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',e=>{const target=document.querySelector(link.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}}));